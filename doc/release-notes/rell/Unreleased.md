## [Unreleased] - 2024-xx-xx

### Breaking 💔

- Removed the `gtv` from `bytes` convertion of the properties `args` and `rules`
  from the mappers `map_account_auth_descriptor`, `map_main_auth_descriptor` and
  `map_auth_descriptor_signer`

### Changed 🪙

### Added ✅

### Fixed 🔧
