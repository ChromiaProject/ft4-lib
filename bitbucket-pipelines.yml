image: node:16

options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - pip install docker-compose
            - npm install
            - npm test

          services:
            - docker
