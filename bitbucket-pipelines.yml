
options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - apt install python3-pip
            - pip install --no-cache-dir docker-compose
            - npm install
            - npm test

          services:
            - docker
