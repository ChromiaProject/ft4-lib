
options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - pip install --no-cache-dir docker-compose
            - npm install
            - npm test

          services:
            - docker
