image: node:16

options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - npm install
            - npm test

          services:
            - docker
