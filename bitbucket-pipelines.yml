
options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - apk add --no-cache py-pip
            - pip install --no-cache-dir docker-compose
            - npm install
            - npm test

          services:
            - docker
