image: node:16

options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - apt update
            - apt install python3-pip
            - pip3 install docker-compose
            - npm install
            - npm test

          services:
            - docker
