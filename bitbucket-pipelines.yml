
options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          script:
            - apt update
            - apt install -y python3-pip
            - pip3 install docker-compose
            - npm install
            - npm test

          services:
            - docker
