image: node

options:
  docker: true

pipelines:
  pull-requests:
    '**':
      - step:
          name: Test
          caches:
            - pip
            - node
          script:
            - apt update
            - apt install -y python3-pip
            - pip3 install docker-compose
            - npm install
            - npm test

          services:
            - docker
