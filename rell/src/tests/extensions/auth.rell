module;

import ^^^.lib.ft3.auth;

@extend(auth.login_config)
function () = auth.add_login_config(
    flags = ["B"]
);

@extend(auth.login_config)
function () = auth.add_login_config(
    name = "custom_config",
    flags = ["X", "Y"]
);

