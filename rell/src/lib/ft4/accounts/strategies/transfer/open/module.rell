module;

import ^^^^.accounts;
import transfer: ^;

@extend(transfer.transfer_strategy)
function () = transfer.add_transfer_strategy(
	name = "open",
	action = transfer_open(*)
);

function transfer_open(accounts.account) {}
