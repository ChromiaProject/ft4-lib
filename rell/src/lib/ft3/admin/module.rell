@mount("ft4.admin")
module;

import ^.account;
import ^.core;
import ^.utils;

struct module_args {
    admin_pubkey: pubkey;
}

function require_admin() {
    require(
        op_context.is_signer(chain_context.args.admin_pubkey),
        "Expected admin signature"
    );
}
