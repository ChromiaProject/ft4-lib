// no filters, no data
// no filters, data
// all filters, no data
// all filters, data

describe("Asset queries using filter", () => {
  it("returns assets", async () => {
    //getAssets
  });
  it("returns asset balances", async () => {
    //getBalances
  });
  it("returns transfer history entries", async () => {
    //getTransferHistoryEntries
  });
  it("returns crosschain transfer history entries", async () => {
    //getCrosschainTransferHistoryEntries
  });
  it("", async () => {});
  it("", async () => {});
  it("", async () => {});
  it("", async () => {});
  it("", async () => {});
  it("", async () => {});
  it("", async () => {});
});
